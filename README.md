# KiTTY Manager 2.0

KiTTY Manager — приложение Windows для SSH-сессий. Оно хранит серверы в группах, находит путь через точки входа и промежуточные серверы, открывает терминал KiTTY, SFTP в WinSCP и веб-панели в Firefox. Для повторяющихся операций есть конструктор массовых задач и встроенный Ansible.

## С чего начать

1. Скачайте **`KiTTYManager-2.0.0-windows-x64.zip`** из [Releases](https://github.com/grusha-time/KiTTY-Manager/releases), распакуйте в доступную для записи папку, например `C:\Tools\KiTTYManager`.
2. Запустите `KiTTYManager.exe`. Устанавливать .NET отдельно для готового ZIP не нужно.
3. Создайте сессию или импортируйте папку portable-сессий KiTTY. Укажите адрес, SSH-порт, логин и пароль/ключ, сохраните карточку.
4. Если сервер доступен с ПК, включите **«Сначала подключаться напрямую, без JH»**. Если нужен вход через другую сеть, настройте [точку входа](wiki/Jumphost.md).
5. Двойной клик по сессии выполняет умное подключение. Для открытия исходной сессии есть отдельное действие **«Открыть напрямую в KiTTY»**.

Полная [установка и первый запуск](install-instruction.md) объясняют оба варианта подключения, обновление и перенос данных. [Главная страница руководства](wiki/Home.md) помогает выбрать нужный раздел. В приложении есть **«Меню → Справка по полям…»** с поиском по всем разделам (`Ctrl+F`).

## Что можно делать

| Задача | Возможность | Руководство |
|---|---|---|
| Навести порядок в сессиях | Группы, подгруппы, поиск, импорт KiTTY, обычное и быстрое дублирование | [Сессии и настройки](wiki/Configuration.md) |
| Дойти до сервера за несколькими переходами | Умные SSH-маршруты, резервные адреса, ограничения внутренних адресов, память рабочего пути | [Маршрутизация](wiki/Routing.md) |
| Поднять вход в закрытую сеть | SOCKS5 через JH, OTP/TOTP, команда доступа, контрольные серверы и расписание | [Точки входа](wiki/Jumphost.md) |
| Проверить связи | Карта, направления, выбор пар и сохранённые результаты | [Карта связей](wiki/Link-Map.md) |
| Открыть панель или передать файл | Firefox с отдельным профилем и внутренним разрешением имён; WinSCP через маршрут | [Веб-панели и SFTP](wiki/Web-Interfaces.md) |
| Выполнить операцию на нескольких серверах | Конструктор из 11 типов действий или обычный playbook Ansible | [Массовые задачи](wiki/Batch-Tasks.md) |
| Перенести настройки | Выборочный JSON-экспорт и мастер умного импорта | [Экспорт и импорт](wiki/Export-Import.md) |

## Что входит в ZIP

- `KiTTYManager.exe` — приложение со встроенным .NET runtime.
- `KiTTY/` — portable KiTTY, её настройки и лицензия.
- `Runtime/Ansible/` — QEMU, Linux runtime, Ansible и лицензии компонентов.

Firefox и WinSCP **не входят** в ZIP: установите их отдельно, если нужны соответствующие функции. Для SSH-консолей они не требуются. Для Ansible нужна работоспособная виртуализация Windows/WHPX; кнопка готовности находится в режиме Ansible. Файлы пользовательских сессий и учётные данные в дистрибутив не включены. Документация и [учебные задачи](examples/batch-tasks/README.md) находятся в репозитории и не входят в релизный ZIP. Для чтения и запуска примеров без доступа к GitHub заранее скачайте исходники репозитория целиком. «Справка по полям» встроена в приложение и доступна без этих файлов.

## Важные особенности

- Конфигурация хранится рядом с приложением в `Data/`. Не запускайте менеджер прямо из архиватора или из папки без права записи.
- Секретные поля `config.json` защищены Windows DPAPI. Для другого компьютера/учётной записи используйте JSON-экспорт: в нём секреты **открытые**, такой файл нельзя публиковать.
- Автоматическое доверие новым и изменённым SSH host key по умолчанию включено. Для ручной сверки отпечатков отключите его в настройках.
- Временный профиль Firefox не синхронизирует новые пароли и исключения сертификатов обратно в исходный профиль.
- Полный выход из менеджера закрывает обслуживаемые им маршруты. Сворачивание в трей оставляет их работающими.

## Если что-то не работает

Откройте [разбор типичных проблем](wiki/Troubleshooting.md). Перед отправкой журнала включите логирование, повторите проблему и проверьте файл на внутренние адреса и секреты ваших команд. Успешная сборка или тесты ядра не заменяют проверку конкретного Windows/SSH-стенда.

Разработчикам: [архитектура](wiki/Architecture.md), [сборка и упаковка](wiki/Building.md), [автотесты и мутации](wiki/Test-Audit.md). GitHub-публикация и локальная сборка — отдельные действия.
