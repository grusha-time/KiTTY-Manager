<Window x:Class="KiTTYManager.App.ThemedMessageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Style="{StaticResource {x:Type Window}}"
        Width="500" MinHeight="210" SizeToContent="Height" MaxHeight="650"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False"
        Background="{StaticResource WindowBrush}" Foreground="{StaticResource TextBrush}" FontFamily="Segoe UI" FontSize="14">
  <Grid Margin="24" Background="{StaticResource WindowBrush}">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <DockPanel>
      <Border x:Name="IconBadge" Width="32" Height="32" CornerRadius="16" Background="#26395C" Margin="0,0,13,0">
        <TextBlock x:Name="IconText" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="17" FontWeight="SemiBold"/>
      </Border>
      <TextBlock x:Name="HeadingText" VerticalAlignment="Center" FontSize="18" FontWeight="SemiBold" TextWrapping="Wrap"/>
    </DockPanel>
    <ScrollViewer Grid.Row="1" MaxHeight="420" VerticalScrollBarVisibility="Auto" Margin="45,16,0,24">
      <TextBlock x:Name="MessageText" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" LineHeight="21"/>
    </ScrollViewer>
    <StackPanel x:Name="ButtonsPanel" Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right"/>
  </Grid>
</Window>
